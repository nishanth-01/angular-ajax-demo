- replace boolean expressions invloving 'NaN' if '0' is a valid value
- throttle button clicks
- improved GUI
- service for input validation
- check for resource leaks
  - unsubscribe observables
- if already logged in show the logged in user info and ask if to switch user, change user button
- move hardcoded global configuration variables to config.json
