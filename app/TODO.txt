- add access control to admin app component
- replace boolean expressions invloving 'NaN' if '0' is a valid value
- throttle button clicks
- service for input validation
- unsubscribe observables
- change user button
- if already logged in show the logged in user info and ask if to switch user
